PySide6>=6.8.0
aiohttp>=3.9
pynput>=1.7.6
pyautogui>=0.9.54
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.26
Pillow>=10.2
mss>=9.0
pywin32>=306; platform_system == "Windows"

# STT — pure ONNX Runtime via sherpa-onnx. Any sherpa-onnx-compatible
# export works. For GPU swap onnxruntime → onnxruntime-gpu
# (setup.ps1 handles this when -GpuSupport is on).
sherpa-onnx>=1.10

# TTS — pure ONNX Runtime via piper-tts (which bundles onnxruntime +
# phonemization). Same GPU swap applies.
piper-tts>=1.2.0

# Model auto-download from HuggingFace (used by both engines when the
# user enters a repo ID instead of a local path).
huggingface_hub>=0.24
